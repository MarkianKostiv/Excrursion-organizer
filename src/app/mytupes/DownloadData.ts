export interface DownloadData {
  data?: {
    id: number;
    author: string;
    tittle: string;
    kind: string;
    topic: string;
    goal: string;
    references: string;
    time: string;
    artifacts: string;
  };
}
